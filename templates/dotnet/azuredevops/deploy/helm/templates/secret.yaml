apiVersion: v1
kind: Secret

metadata:
  name: {{ .Release.Name }}-secret
  labels:
    app: {{ .Release.Name }}

type: Opaque

{{- if .Values.secret }}
stringData:
{{- range $key, $value := .Values.secret }}
  {{ $key }}: "{{ $value }}"
{{- end }}
{{- end }}
