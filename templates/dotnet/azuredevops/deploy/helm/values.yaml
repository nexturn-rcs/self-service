replicaCount: 1

image:
  repository: ""
  tag: ""

service:
  type: LoadBalancer
  port: 80
  targetPort: 8080

container:
  port: 8080

resources:
  requests:
    cpu: 100m
    memory: 128Mi

  limits:
    cpu: 500m
    memory: 512Mi

ingress:
  enabled: false
  className: nginx

  annotations: {}

  hosts:
    - host: ""
      paths:
        - path: /
          pathType: Prefix

  tls: []

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5

  targetCPUUtilizationPercentage: 80

livenessProbe:
  enabled: true
  path: /health
  initialDelaySeconds: 30
  periodSeconds: 10

readinessProbe:
  enabled: true
  path: /health
  initialDelaySeconds: 15
  periodSeconds: 5

config:
  APP_NAME: ${{ values.repoName }}
  ASPNETCORE_ENVIRONMENT: Production

secret: {}

env: {}

podAnnotations: {}

nodeSelector: {}

tolerations: []

affinity: {}
