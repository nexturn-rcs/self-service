server {
    listen 80;

    location /health {
        default_type text/plain;
        return 200 'OK';
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
